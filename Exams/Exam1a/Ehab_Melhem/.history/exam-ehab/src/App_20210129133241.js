import "./App.css";
import {const [state, setstate] = useState(initialState)}
function App() {
  return (
    <div className="app">
      <h1>let's build app</h1>
      <h4>{}</h4>
    </div>
  );
}

export default App;
